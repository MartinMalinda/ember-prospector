import relationshipChecker from 'dummy/utils/relationship-checker';
import { module, test } from 'qunit';

module('Unit | Utility | relationship-checker', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = relationshipChecker();
    assert.ok(result);
  });
});
